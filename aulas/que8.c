#include <stdio.h>
int main(){
    int max, atual, a=0, b=1;
    printf("insira a sequencia de fibanacci que voce quer: ");
    scanf("%d", &n);
    for (int i =0; i < n; i++)
    {
        printf("%d", i);

    }
    printf("\n");
    
}